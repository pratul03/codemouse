import { Button } from "../ui/button";

export default function Navbar() {
  return (
    <div>
      <div>
        <Button variant={"secondary"} size={"sm"}>
          Navbar
        </Button>
      </div>
    </div>
  );
}
